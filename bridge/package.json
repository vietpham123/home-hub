{
  "name": "smarthouse-bridge",
  "version": "1.0.0",
  "description": "Tuya/Gosund to MQTT bridge for Smarthouse app",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js",
    "dev": "node bridge.js --verbose"
  },
  "dependencies": {
    "tuyapi": "^7.5.1",
    "mqtt": "^5.3.0"
  }
}
